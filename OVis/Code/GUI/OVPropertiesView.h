/*!	@header		OVPropertiesView.h
	@discussion Custom view for the Properties Window. All it does is override
				isFlipped to attach the scroll view to the top instead of the
				bottom.
	@author		Thomas Höllt
	@updated	2013-07-29 */

#import <Cocoa/Cocoa.h>

/*!	@class		OVGLView
	@discussion	Custom view for the Properties Window. All it does is override
				isFlipped to attach the scroll view to the top instead of the
				bottom.*/
@interface OVPropertiesView : NSView{
	
}

@end
